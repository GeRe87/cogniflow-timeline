# Weekly contributions

Author-specific inputs are stored as:

`<YEAR>/W<WEEK>/<author>.json`

Each file must conform to `schemas/contribution.schema.json`. Contributions are
internal editorial inputs within this public repository; they must already be
safe for public disclosure.
